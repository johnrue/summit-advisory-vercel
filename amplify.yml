version: 1
frontend:
  phases:
    preBuild:
      commands:
        # Use Node.js 20 for AWS Amplify compatibility
        - nvm use 20
        # Use npx to run pnpm without global installation
        - npx pnpm install
    build:
      commands:
        # Build the Next.js static export using npx
        - npx pnpm run build
  artifacts:
    # For Next.js static export, the output directory is 'out'
    baseDirectory: out
    files:
      - '**/*'
  cache:
    paths:
      - node_modules/**/*
      - .next/cache/**/*
  # Set NODE_OPTIONS as environment variable for memory optimization
  environment:
    variables:
      NODE_OPTIONS: '--max-old-space-size=8192'
